import React from 'react';
import { useSelector } from 'react-redux';

function Display(){

    // const count = useSelector((state) => state.counter.value)
    // const mul = useSelector((state) => state.counter.multiple)
    // const colour=useSelector((state)=>state.counter.bgcolor)




    return(
        <>
{/* <div className={`${colour==true?"bg-yellow-400":"bg-blue-500"} h-full w-full`}>
    <h1>Count:{count}</h1>
<h1>Multiplied Count:{mul}</h1>

</div> */}

        </>
    )
}
export default Display;